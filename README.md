Forgot Password button
name == "Forgot Password?"
xpath
//XCUIElementTypeButton[@name="Forgot Password?"]

Email input box
name == "Email"
xpath
//XCUIElementTypeTextField[@name="Email"]

Send Message button
name == "SEND MESSAGE"
xpath
//XCUIElementTypeButton[@name="SEND MESSAGE"]

Invalid email error message
name == "Please enter valid email id"
xpath
//XCUIElementTypeStaticText[@name="Please enter valid email id"]

Verification Code page
name == "ENTER VERIFICATION CODE"
xpath
//XCUIElementTypeStaticText[@name="ENTER VERIFICATION CODE"]

Verify button
name == "VERIFY"
xpath
//XCUIElementTypeButton[@name="VERIFY"]

Failed to verify OTP dialog
name == "FAILED TO VERIFY OTP"
xpath
//XCUIElementTypeStaticText[@name="FAILED TO VERIFY OTP"]

OTP error message
name == "Please try again later."
xpath
//XCUIElementTypeStaticText[@name="Please try again later."]

OK button
name == "OK"
xpath
//XCUIElementTypeButton[@name="OK"]

Get a new code link
name == "Get a new code"
xpath
//XCUIElementTypeStaticText[@name="Get a new code"]

Resend Successful dialog
name == "RESEND SUCCESSFUL"
xpath
//XCUIElementTypeStaticText[@name="RESEND SUCCESSFUL"]

Resend success message
name == "Verification code has been send successfully."
xpath
//XCUIElementTypeStaticText[@name="Verification code has been send successfully."]

Reset Password page
name == "RESET PASSWORD" AND label == "RESET PASSWORD" AND value == "RESET PASSWORD"
xpath
//XCUIElementTypeStaticText[@name="RESET PASSWORD"]

Enter Password field
name == "Enter Password"
xpath
//XCUIElementTypeOther[@name="Enter Password"]

Confirm Password field
name == "Confirm Password"
xpath
//XCUIElementTypeOther[@name="Confirm Password"]

Reset Password button
name == "RESET PASSWORD" AND label == "RESET PASSWORD" AND type == "XCUIElementTypeButton"
xpath
//XCUIElementTypeButton[@name="RESET PASSWORD"]

Password validation error
name == "Use at least 8 characters with uppercase, lowercase, number, and special symbol."
xpath
//XCUIElementTypeStaticText[@name="Use at least 8 characters with uppercase, lowercase, number, and special symbol."]

Passwords do not match error
name == "Passwords do not match"
xpath
//XCUIElementTypeStaticText[@name="Passwords do not match"]

Sign In page
name == "SIGN IN"
xpath
//XCUIElementTypeStaticText[@name="SIGN IN"]


correctly replace the xpaths,
if xpath is not there in automation time tab ,clike,verify using use name 

give it corrrectly and run







value == "6"
xpath
//XCUIElementTypeTextField[@value="6"]
